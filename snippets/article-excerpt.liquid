{%- liquid
if excerpt_length == blank
  assign excerpt_length = 40
endif

if article
  if article.excerpt != blank
    echo article.excerpt
  elsif article.content
    echo article.content | strip_html | truncatewords: excerpt_length, '...'
  endif
endif
-%}
